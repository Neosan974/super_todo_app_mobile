name: super_todo_app_mobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  cupertino_icons: ^1.0.8
  drift: ^2.16.0
  drift_flutter: ^0.2.8
  equatable: ^2.0.8
  flutter:
    sdk: flutter
  get_it: ^9.2.0
  path: ^1.9.0
  path_provider: ^2.1.0
  provider: ^6.1.5+1
  sqlite3_flutter_libs: ^0.5.0

dev_dependencies:
  build_runner: ^2.10.5
  drift_dev: ^2.16.0

  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
